{
  "name": "flapverse-neon-flight",
  "version": "1.0.0",
  "private": true,
  "description": "Landscape-first native mobile arcade game for Android and iPhone",
  "scripts": {
    "start": "node server.mjs",
    "native:sync": "capacitor sync",
    "android:build": "npm run native:sync && cd android && gradlew.bat assembleDebug",
    "android:install": "npm run android:build && adb install -r android/app/build/outputs/apk/debug/app-debug.apk"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.1",
    "@capacitor/core": "^6.2.1",
    "@capacitor/ios": "^6.2.1"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.1"
  }
}
